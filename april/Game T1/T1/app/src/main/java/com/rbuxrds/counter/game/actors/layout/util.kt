package com.rbuxrds.counter.game.actors.layout

enum class AlignH { LEFT, CENTER, RIGHT, SPREAD }
enum class AlignV { TOP, CENTER, BOTTOM, SPREAD }
enum class DirectionV { DOWN, UP }
enum class DirectionH { RIGHT, LEFT }